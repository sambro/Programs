#include <stdio.h>

int main()
{
    int i,n;
    int A[i];


    printf("How many elements do you want to input: ");
    scanf("%d",&n);

    printf("Enter the elements: ");
    for(i=0;i<n;i++)
    {
        scanf("%d",&A[i]);
    }

    printf("the elements are: %d\n",A[i]);

    return 0;

}
